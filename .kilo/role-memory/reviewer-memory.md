# Reviewer Memory

## Review Patterns
- [2026-07-14] **Tailwind v4 design-system PRs:** verify tokens via `getComputedStyle` (computed styles trong browser), KHÔNG chỉ đọc CSS source. `@theme inline` mapping có thể đúng trong CSS nhưng không sinh utility nếu sai cú pháp. Check: body backgroundColor, color, logo color, font-family. VD Issue #1: light bg rgb(251,250,246)=#fbfaf6, dark bg rgb(26,26,26)=#1a1a1a — match design doc section 8.2.
- [2026-07-14] **Theme toggle verification:** test 3 thứ — (1) click toggle → data-theme attr đổi + tokens swap, (2) localStorage persist, (3) reload → init script restore (FOUC prevention). Dùng Playwright browser_evaluate để check computed styles sau mỗi bước. VD Issue #1.
- [2026-07-14] **"Installed + basic via starter-kit" AC:** satisfied bởi package.json + starter-kit bundled extensions (bold/italic/heading trong lockfile). KHÔNG block foundation PR vì thiếu editor UI rendered — editor là scope UF-4. Nhược note như MINOR follow-up.
- [2026-07-14] **FOUC-free theme pattern (Tailwind v4 + Next.js Server Component):** blocking script trong head set data-theme từ localStorage trước paint + suppressHydrationWarning trên html. @custom-variant dark thay vì prefers-color-scheme. Pattern đúng, accept.
- [2026-07-14] **deepagents middleware PRs (BYOK/`@wrap_model_call`):** KHÔNG chỉ verify import OK. Phải verify 3 cấp: (1) WIRING — đọc graph.py source: `if middleware: deepagent_middleware.extend(middleware)` (line 811-812) → `create_agent(middleware=...)` (line 869). `agent.middleware` attr = None là BÌNH THƯỜNG (middleware lưu trong closure, không expose qua __dict__). (2) FUNCTIONAL — test `_resolve_byok_model` với mock context: None/empty/no-model → None (fallthrough), {model+api_key} → ChatOpenRouter. (3) API MATCH — verify `ModelRequest.runtime` (Runtime[ContextT]) + `override(model=...)` (docstring xác nhận `model` là override key hợp lệ). VD Issue #2.
- [2026-07-14] **None-guard pattern cho framework-injected objects:** Khi access `request.runtime.context`, framework (LangGraph factory.py:1433 `model_node(state, runtime: Runtime)`) LUÔN inject non-None Runtime. NHƯNG nếu ModelRequest được construct với `runtime=None` (unit test, manual) → `request.runtime.context` crash AttributeError. Pattern đúng: `rt = request.runtime; ctx = rt.context if rt is not None else None`. Latent trong production (MINOR) nhưng phải detect + note. VD Issue #2: `_resolve_byok_model` đã handle `context is None` NHƯNG caller `wrap_model_call` không guard `runtime is None`.
- [2026-07-15] **.kilo/ infra scripts in PR diff:** Agent Manager worktree setup (run-script.ps1, setup-script.ps1) may appear as NEW files in PR diff outside EDIT ZONE. Note as MINOR (no application impact) but flag for Manager / Worker to clarify whether these are Worker-authored or system-generated.

- [2026-07-15] **deepagents StoreBackend namespace factory PRs:** verify KHONG chi unit-test mock — phai invoke factory voi REAL `_NamespaceRuntimeCompat(None)` (deepagents store.py:73-119) de confirm graceful fallback trong production outside-graph path. `__getattr__` raise AttributeError khi `self._runtime is None` → factory `try: ctx = rt.context except AttributeError: return default` la DUNG. Verify 3 cap: (1) WIRING — `routes['/manuscript/']._namespace is agent._manuscript_namespace` (attr that tai store.py:229 `self._namespace = namespace`); (2) API MATCH — `NamespaceFactory = Callable[[Runtime], tuple[str,...]]` (store.py:124); (3) VALIDATION — `_validate_namespace` (store.py:131) reject empty tuple/non-str/empty str/chars ngoai `^[A-Za-z0-9\-_.@+:~]+$` → fallback values 'default'/'draft'/'novels'/'memories' deu pass. Latent MINOR: malformed user_id (ky tu dac biet) pass factory (str(value)) NHUNG `_validate_namespace` raise — production user_id=UUID luon valid. VD Issue #14 PR #17.
## Common Rejection Reasons
<!-- Ghi khi reject -->

## Lessons Learned
- [2026-07-14] **`gh pr checkout` fail khi branch đã trong worktree:** error "already used by worktree at .kilo/worktrees/...". Fix: làm việc trực tiếp trong worktree dir (git -C <worktree>, chạy build/dev trong worktree/frontend). VD Issue #1.
- [2026-07-14] **Process gates (R2.0 self-review, R2.3 visual proof) thường bị Worker bỏ sót:** PR body chỉ "Closes #N" mà thiếu /local-review: PASS + VISUAL PROOF section. Nếu technical work pass tất cả AC + behavior verified independently → APPROVE với note, KHÔNG circular reject. Nhưng báo Manager nhắc Worker tuân thủ cho PR sau.
- [2026-07-14] **Kanban move post-merge:** dùng `gh project item-edit` với project-id từ `gh project list` (KHÔNG phải field-list), status field ID + Done option ID từ `gh project field-list`. Item ID từ `gh project item-list` filter theo content.number. VD Issue #1: Done=98236657.
- [2026-07-14] **doc-sync detect-only cho foundation PRs:** Khi review BE foundation PR (Issue #2), doc-sync phát hiện 2 drift: (1) AGENTS.md techstack KHÔNG list `langchain-openrouter` package (chỉ mention `openrouter:z-ai/glm-5.2` model spec) → báo Planner TECHSTACK DRIFT update shared.md/AGENTS.md. (2) ARCHITECTURE.md Appendix B mention `/codex/` route trong CompositeBackend NHƯNG PR chỉ implement /manuscript/+/memories/+/skills/ (codex = Postgres structured per Section 3.1, KHÔNG phải file route) → doc inconsistency, báo Planner clarify Appendix B. Drift MINOR, KHÔNG block merge (Issue AC match code).
- [2026-07-15] **Scaffold AC slipping (UF-1a #1, UF-1b #11):** FE PR insert chi 1 DB row roi claim/close Issue co AC scaffold manuscript+codex+memory. Scaffold that = tao FILES (manuscript/codex/memories) can BE agent trigger. Verify: grep backend scaffold|on_create|trigger neu rong -> GAP. FE sub-task KHONG duoc Closes #N parent neu scaffold AC con no -> Partially addresses #N + follow-up issue. VD PR #11: form insert chi row, 0 scaffold -> REJECT vi Closes #3 sai.
- [2026-07-15] **Tailwind v4 dark accent (Direction A):** text-vermilion mot minh KHONG swap terracotta o dark (globals.css --vermilion #a93226 giong nhau 2 mode, chi --terracotta #c47a5a khac). Muon terracotta dark -> phai text-vermilion dark:text-terracotta (layout.tsx da dung). Verify getComputedStyle .text-vermilion: header logo=terracotta(dung), dashboard labels=vermilion(sai dark). VD PR #11 thieu dark:text-terracotta -> MINOR.
- [2026-07-15] **Behavior verify can dung branch:** dev server tu main checkout render OLD code. PHAI chay dev trong worktree .kilo/worktrees/<branch>/frontend. git worktree list -> SHA match PR head. VD PR #11: main="To get started...", worktree="Tieu thuyet cua toi".
- [2026-07-15] **Re-review: verify all fixes applied before merge.** Khi Worker claims fixes applied, GREP actual source files (KHÔNG chỉ trust diff). Đếm từng instance. VD PR #11 re-review: Worker claimed 5+3 dark:text-terracotta — grep confirm chính xác 5 trong page.tsx (line 63,84,97,189,203) + 3 trong novels/new/page.tsx (line 115,125,217). Merge only when ALL claimed fixes verified in source.
- [2026-07-15] **Sub-task "Closes #N" auto-closes parent Issue:** When a sub-task PR (e.g., BE UF-1a) has "Closes #N" in commit body, GitHub auto-closes Issue #N even if other sub-tasks (FE form, selector) remain outstanding. Reviewer must verify Issue AC completeness BEFORE merge. If parent Issue has remaining AC → add comment + re-open immediately after merge. VD PR #13: BE scaffold done but Issue #3 still needs FE form + technique selector → re-opened.
- [2026-07-15] **Service role key for Supabase with app-layer user_id filtering:** codex_service uses SUPABASE_SERVICE_ROLE_KEY (bypasses RLS) + manually filters by user_id in each query (.eq("user_id", user_id)). This is correct for agent tools (server-side context, no JWT). Reviewer must verify: (1) user_id comes from runtime.context (not user input), (2) every CRUD function filters by user_id, (3) security boundary raises ValueError when context missing. VD PR #13.
- [2026-07-15] **Commit-order impl-first cho bisect-safety KHONG phai test gaming:** PR #17 commit cd4a27c (impl) TRUOC 2d8de1d (test). Gate R2.4d flag 'test modified after impl' la suspicious, NHUNG impl-first la bisect-safe strategy (ca 2 commit de tree green: impl-only=22 baseline pass, +test=35 pass). Dao nguoc (test-first commit) = tree RED tai test-only commit = KHONG bisect-safe. TDD workflow (RED->GREEN) xay ra trong dev, commits ordered cho bisect-safety. Phan bich: commit order != TDD workflow order. Accept neu tests mutation-resistant (exact `==` assertions, khong assertTrue/contains/skip). VD Issue #14.
- [2026-07-15] **Doc-sync drift pre-existing + outside EDIT ZONE → flag, KHONG block:** R0 gate strict-reading noi REJECT on drift, NHUNG khi (1) drift PRE-EXISTING (doc duoc viet truoc voi assumption cu, KHONG phai PR nay introduce), (2) code match Issue spec + memory constraint (authoritative), (3) doc fix outside EDIT ZONE (Worker khong duoc sua), (4) Worker flag transparent — thi APPROVE + bao Manager run doc-sync post-merge. Review against Issue spec + memory, KHONG against stale doc. VD Issue #14: ARCHITECTURE.md §3.2 line 127 doc 4-tuple (user_id,'novels',novel_id,'manuscript') NHUNG code+Issue spec+memory=3-tuple (user_id,'novels',novel_id). §3.3 Memory khong doc namespace NHUNG code=(user_id,'memories'). Drift MINOR, code dung.
